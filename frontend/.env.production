# Public build-time config for Calistique (safe to commit — all values are public).
# Vite root is `frontend/`, so production builds read VITE_* vars from this file.

# Google Analytics 4
VITE_GA4_MEASUREMENT_ID=G-DH1WBK8YWK

# Google AdSense (publisher ID is public — also present in /ads.txt and page source)
VITE_ENABLE_ADS=true
VITE_ADSENSE_CLIENT_ID=ca-pub-5800977493749262
